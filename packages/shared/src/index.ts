export * from "./money";

/**
 * Placeholder for OpenAPI-generated Zod schemas (Phase 0 non-negotiable rule:
 * frontend validation must be generated from the backend's OpenAPI spec, not
 * hand-written, so the two cannot drift). Real generation wires up once
 * apps/api exposes an OpenAPI document — tracked in docs/DECISIONS.md.
 */
export {};
